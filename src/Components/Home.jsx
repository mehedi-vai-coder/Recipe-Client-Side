import React from 'react';
import Slider from '../Slider/Slider';

const Home = () => {
    return (
        <div>
            <section>
                 <Slider></Slider>
            </section>
        </div>
    );
};

export default Home;